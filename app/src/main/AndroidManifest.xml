<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeActivity"
        tools:targetApi="31"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".compose.ReminderScreen"
            android:exported="false"
            android:label="@string/title_activity_reminder_screen"
            android:theme="@style/Theme.ComposeActivity" />
        <activity
            android:name=".compose.ConfigurationMain"
            android:exported="false"
            android:label="@string/title_activity_configuration_main"
            android:theme="@style/Theme.ComposeActivity" />

        <receiver
            android:name=".viewmodel.NotificationReceiver"
            android:exported="true" />

        <activity
            android:name=".compose.ui.theme.Utility"
            android:exported="false"
            android:label="@string/title_activity_utility"
            android:theme="@style/Theme.ComposeActivity" />
        <activity
            android:name=".compose.SpecjalizationScreen"
            android:exported="false"
            android:label="@string/title_activity_specjalization_screen"
            android:theme="@style/Theme.ComposeActivity" />
        <activity
            android:name=".compose.MainScreen"
            android:exported="false"
            android:label="@string/title_activity_main_screen"
            android:theme="@style/Theme.ComposeActivity" />
        <activity
            android:name=".compose.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ComposeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="default_channel" />
    </application>

</manifest>